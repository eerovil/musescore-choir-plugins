{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Test: Run Pytest",
            "type": "shell",
            "command": ".venv/bin/python -m pytest src/clean_score/tests/ --log-cli-level=DEBUG",
            "problemMatcher": [],
            "presentation": {
                "reveal": "always"
            },
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}
